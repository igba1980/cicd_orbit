image:
  repository: iujege/cicd_orbit
  tag: 0373b5d
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true

serviceAccount:
  enabled: true

autoscaling:
  enabled: true